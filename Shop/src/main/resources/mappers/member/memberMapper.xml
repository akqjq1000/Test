<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.idu.shop.MemberMapper">
    <select id="readList" resultType="com.idu.shop.domain.member.Member">
        <![CDATA[select * from member]]>
    </select>

    <select id="login" resultType="com.idu.shop.domain.member.Member">
        SELECT * FROM member WHERE email=#{email} AND password=#{password}
    </select>

    <select id="readByEmail" resultType="com.idu.shop.domain.member.Member">
        SELECT * FROM member WHERE email=#{email}
    </select>
    <select id="readByEmailOnId" resultType="int">
        SELECT id FROM member WHERE email=#{email}
    </select>
    <select id="read" resultType="com.idu.shop.domain.member.Member">
        SELECT * FROM member WHERE id=#{id}
    </select>
    <select id="findEmail" resultType="com.idu.shop.domain.member.Member">
        SELECT * FROM member WHERE name=#{name} AND phone=#{phone}
    </select>
    <insert id="create">
        INSERT INTO member VALUES(seq_member.nextval, #{email}, #{password}, #{name}, #{phone}, #{address}, #{point}, SYSDATE)
    </insert>
    <update id="update">
        update member set name=#{name}, phone=#{phone}, address=#{address}, point=#{point} where email=#{email}
    </update>
    <update id="updatePw">
        update member set password=#{password}, name=#{name}, phone=#{phone}, address=#{address}, point=#{point} where email=#{email}
    </update>
    <delete id="delete">
        delete from member where email = #{email} AND password = #{password}
    </delete>
</mapper>